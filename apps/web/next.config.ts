import type { NextConfig } from "next";

// Minimal App Router config. Image rewrites, redirects, and security headers
// are added in later milestones (M2 Core Layout) — kept intentionally lean here.
const nextConfig: NextConfig = {
  reactStrictMode: true,
};

export default nextConfig;
