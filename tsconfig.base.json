{
  "$schema": "https://json.schemastore.org/tsconfig",
  "//": "Canonical strict base now lives in @nexus/tsconfig. This root copy is retained so existing relative 'extends: ../../tsconfig.base.json' references keep working. Edit the package, then mirror here.",
  "extends": "./packages/tsconfig/tsconfig.base.json"
}
